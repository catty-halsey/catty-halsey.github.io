Decision is a mapping we choose to estimate our data. Note that each decision is like a rule which specifies which part of data or the combination of data should map to a element in the action space.
